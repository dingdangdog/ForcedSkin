# 003 — Extension German locale (de)

- Added `extension/_locales/de/messages.json` with full German translation of all 112 Chrome extension i18n keys from `en/messages.json`.
- Preserved placeholders (`{host}`, `{mode}`, `{count}`, `{1}`, `$1`), brand names (ForcedSkin, forcedskin.com, GitHub, Google), and layer kind prefixes (surface, accent, canvas, richText, svgRecolor).
